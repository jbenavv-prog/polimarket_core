import 'package:flutter/material.dart';

class SolicitarProductoScreen extends StatelessWidget {
  const SolicitarProductoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Solicitar Producto')),
    );
  }
}
